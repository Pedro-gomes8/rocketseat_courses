/* 
    Prototypes
    __proto__
  
    Ver todos os metodos atraves de .__proto__
*/

let string='Calafrio';
let numero= 345784;
console.log(`A palavra ${string} possui ${string.length} caracteres\n
O Numero ${numero} possui ${String(numero).length} digitos
`);